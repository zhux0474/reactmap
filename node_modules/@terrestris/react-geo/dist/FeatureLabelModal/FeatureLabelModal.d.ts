import * as React from 'react';
import { ModalProps } from 'antd';
import Feature from 'ol/Feature';
import Geometry from 'ol/geom/Geometry';
declare type OwnProps = {
    feature: Feature<Geometry>;
    onOk: () => void;
    onCancel: () => void;
    /**
     * Maximal length of feature label.
     * If exceeded label will be divided into multiple lines. Optional.
     */
    maxLabelLineLength?: number;
};
export declare type FeatureLabelModalProps = OwnProps & Omit<ModalProps, 'closable' | 'visible' | 'onOk' | 'onCancel'>;
export declare const FeatureLabelModal: React.FC<FeatureLabelModalProps>;
export {};
